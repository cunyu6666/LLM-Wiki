---
id: "6a2bed440000000006022a92"
source: 小红书
author_id: "605c4c6f00000000010081c0"
url: https://www.xiaohongshu.com/explore/6a2bed440000000006022a92?xsec_token=AB5tR21seiJs92_Qsp_-GDGzetGDZGaokP4fdn0LIAP4k=&xsec_source=pc_collect
tags: ["AI Agent 开发", "AI", "批量入库"]
liked: 48
collected: 51
comments: 2
images: 4
ocr_engine: macOS Vision API (VNRecognizeTextRequest, accurate, zh-Hans + en)
captured: 2026-06-20
batch: "xhs mega batch #1 (10 notes)"
---

# Claude Code 30万行代码的核心

> 抓取日期：2026-06-20
> 数据：48 赞 / 51 收藏 / 2 评论 / 4 图
> 作者 ID：`605c4c6f00000000010081c0`（未登录态无昵称）
> OCR：macOS Vision API

---

## 原文摘录

> Claude Code 的核心，其实就是一个 while 循环：模型生成响应，如果里面有工具调用，就执行工具，把结果塞回上下文，再让模型生成下一步，持续往复。
	
就这么一个循环，被工程化成了完整产品，代码规模 30 多万行。
	
从设计上看，Claude Code = 模型 + Harness。Harness 又可以拆成三件事：工具系统、上下文工程、自主循环。
工具系统里，CC 做了很多标准化约束。它不会鼓励模型直接执行 find、grep、cat、head 这类通用行为，而是引导模型走 GrepTool、GlobTool 等专用工具。原因是内建工具可以输出可审计、结构化的日志，让每一步行为更透明，也更容易被约束。
	
工具本身也有权限级别和校验逻辑。比如 Edit 为了避免交叉覆盖，会要求先 Read；Git 遇到 push force 这类高风险行为，会有 prompt 约束和 UI...

---

## OCR 全文（4 张图）

### 第 1 张图

![[imgs/6a2bed440000000006022a92/1.jpg]]

Barret李靖
@Barret_China
Claude Code 的核心是一个 while 循环：模型生成响应—如果包含工具调用，执行—结果返回
一模型生成下一个响应 一持续循环。
就这么一个循环，被工程化成一个完整的产品，写了将近30多万行代码。
从整体代码设计来看，可以认为，Claude Code=
模型+Harness，而 Harness=工具系统X上下文工程× 自主循环。
其中，工具系统和上下文工程做了大量的设计。
CC 的工具系统有着自己的标准化设计，它会明确约束模型不要执行 find、grep、cat、head 通用
操作，而是走 GrepTool、GlobTool 等专用工具，因为这些内建工具会输出可审计、结构化的日志，
让操作更加透明可控。

### 第 2 张图

![[imgs/6a2bed440000000006022a92/2.jpg]]

Edit 工具为了避免交叉覆盖，会要求先 Read；Git同时，工具本身也带有权限级别和验证逻辑。例如
工具对 push force 类高风险操作会做 prompt约束和 UI警告。
和反馈机制，让模型在调用时有约束、有校验，减类似的设计很多，目的是在工具层建立清晰的边界
少越界操作和错误扩散。
而在上下文管理上，CC的管控也无所不用其极。它通过多种压缩策略和社连机制，确保模型在任何
时刻只接触当前任务最相关的信息。
AutoCompact，以及不同触发条件下的会话压压缩策略的核心机制包括 MicroCompact、
缩、记忆替换和裁剪策路。
在文件加载机制上，针对工具定义与能力暴露，也设计了 Just-In-Time策略，文件不预加载，只保
留路径，需要时再通过工具读取。

### 第 3 张图

![[imgs/6a2bed440000000006022a92/3.jpg]]

此外，还有Sub-Agent的设计，它通过上下文隔离的方式，让不同子任务的相关信息互不干扰，进
一步降低了主循环的认知负载，确保主循环逻辑干净且稳定。
Claude Code 不仅是在工具系统和上下文管理上
做文章，模型为了 Harness 效果更好，也开始配合对 Agentic 行为做专项优化。
例如 Opus 4.7 在指令遵循上就明确提到 "Opus4.7 takes the instructic:sgiterally"，这对 Agent
来说非常关键。Agent的行为边界往往写在system prompt 里，模型层做了增强学习后，模
稳定性和可靠性会有极大提升。型在指令遵守方面会表现更出色，这对 Agent的
OpenClaw/Hermes Agent/Claude Code 产生了大量 Agent调用数据，这些数据也会继续反哺模
型能力的迭代。
从当前发展趋势可以推断，未来模型的进化，一定

### 第 4 张图

![[imgs/6a2bed440000000006022a92/4.jpg]]

也会逐步内化工具调用策略、上下文压缩策略，甚至学会自我约束行为边界。
那么，今天CC里写的这些 Harness 逻辑，注定也会被模型吃掉。也就是说，Harness 也是一个过渡
性的产物。§
下午7:09•2026年6月12日
DS
小红书

---

## 抓取与 OCR 说明

- 抓取方式：`curl` + iPhone Safari UA + Referer（无需登录）
- HTML 提取：`__INITIAL_STATE__` 中 `undefined` 替换为 `null`，括号状态机解析
- 图片下载：curl 直连 `sns-webpic-qc.xhscdn.com`
- OCR：macOS Vision API（`VNRecognizeTextRequest`，accurate 级别，zh-Hans + en）
- 阅读顺序：按视觉坐标 `y` 降序 + `x` 升序重建

## 元数据

- 抓取日期：2026-06-20
- 点赞/收藏/评论：48 / 51 / 2
- 图片数：4 张（已存档 `imgs/6a2bed440000000006022a92/1.jpg` ~ `4.jpg`）
- 作者 ID：`605c4c6f00000000010081c0`（未登录态无法解析昵称）

[[Claude Code 30万行代码的核心|wiki 索引版]]
